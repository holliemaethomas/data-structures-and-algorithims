let arr = ["a", "b", "a", "d", "a"];
 function findRepeatWord(arr) {
  for(var i = 0; i < arr.length; i++){
  var firstWord = arr[i];
  for(var j = (i + 1); j < arr.length; j++){
  var nextWord = arr[j];
  }
  }
  if(firstWord === nextWord)
    return nextWord;
 };
 

 findRepeatWord(arr);

